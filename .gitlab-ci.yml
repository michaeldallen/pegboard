image: bbassett/openscad

stages: 
  - diag
  - build
  
diagnostics:
  stage: diag
  script:
    - uname -a
    - uname -s

    
create stl from scad:
  stage: build
  script:
    - openscad --help
    - pwd
    - ls -l 
    - echo Finny is here
    - date
    - make artifacts/inner_hook_plate-1x1.stl
    - date
  artifacts:
    paths:
      - artifacts
    
    

image: bbassett/openscad

stages: 
  - build
  
    
create stl from scad:
  stage: build
  script:
    - make --version
    - gmake
    - openscad --help
    - pwd
    - ls -l 
    - echo Finny is here
    - date
    - make artifacts/inner_hook_plate-1x1.stl
    - date
    - make tidy
  artifacts:
    paths:
      - artifacts
    
    

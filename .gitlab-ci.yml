image: bbassett/openscad

stages: 
  - build
  
create stl from scad:
  stage: build
  script:
    - openscad --help
    - pwd
    - ls -l 
    - echo Finny is here
    - date
    - mkdir -pv artifacts
    - time openscad -o artifacts/inner_hook_plate-1x1.stl inner_hook_plate-1x1.scad
    - date
  artifacts:
    paths:
      - artifacts
    
    
